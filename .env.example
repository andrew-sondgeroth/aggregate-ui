# API base URL (defaults to https://aggregateapi-production.up.railway.app if not set)
VITE_API_BASE_URL=http://localhost:8080

# Demo API key for live demo and explore/search pages
# A fallback key is hardcoded for convenience — override here for production
VITE_DEMO_API_KEY=your-api-key
